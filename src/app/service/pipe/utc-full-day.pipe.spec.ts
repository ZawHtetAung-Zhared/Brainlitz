import { UtcFullDayPipe } from './utc-full-day.pipe';

describe('UtcFullDayPipe', () => {
  it('create an instance', () => {
    const pipe = new UtcFullDayPipe();
    expect(pipe).toBeTruthy();
  });
});
