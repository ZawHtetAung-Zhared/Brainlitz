import { RemainingLessonDatePipe } from './remaining-lesson-date.pipe';

describe('RemainingLessonDatePipe', () => {
  it('create an instance', () => {
    const pipe = new RemainingLessonDatePipe();
    expect(pipe).toBeTruthy();
  });
});
