import { UtcDateAndDayPipe } from './utc-date-and-day.pipe';

describe('UtcDateAndDayPipe', () => {
  it('create an instance', () => {
    const pipe = new UtcDateAndDayPipe();
    expect(pipe).toBeTruthy();
  });
});
