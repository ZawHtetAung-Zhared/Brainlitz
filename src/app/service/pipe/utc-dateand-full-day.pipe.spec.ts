import { UtcDateandFullDayPipe } from './utc-dateand-full-day.pipe';

describe('UtcDateandFullDayPipe', () => {
  it('create an instance', () => {
    const pipe = new UtcDateandFullDayPipe();
    expect(pipe).toBeTruthy();
  });
});
