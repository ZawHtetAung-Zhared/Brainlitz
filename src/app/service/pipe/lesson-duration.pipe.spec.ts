import { LessonDurationPipe } from './lesson-duration.pipe';

describe('LessonDurationPipe', () => {
  it('create an instance', () => {
    const pipe = new LessonDurationPipe();
    expect(pipe).toBeTruthy();
  });
});
